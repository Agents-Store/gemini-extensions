description = "Quick-capture an improvement idea without interrupting work — saves to backlog for later processing during wrap-up Arguments: Quick note (e.g., 'directus-dev missing cache gotcha', 'add VERCEL_TOKEN to L1 .env.example')."
prompt = """
Read the capture skill at `${extensionPath}/skills/capture/SKILL.md` and follow its instructions.

Pass the user's argument as the improvement note. If no argument was provided, ask what to capture."""
