description = "Unified improvement — auto-routes fixes to the correct plugin or parent template based on what kind of knowledge it is Arguments: Description of what to improve (e.g., 'directus-dev sdk-patterns needs cache gotcha', 'add VERCEL_TOKEN to .env.example')."
prompt = """
Read the improve skill at `${extensionPath}/skills/improve/SKILL.md` and follow its instructions.

Pass the user's argument as the improvement description. If no argument was provided, ask what needs improving."""
